<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.staysee.forcefitness">

    <application
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity android:name=".MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:parentActivityName=".MainMenuActivity"></activity>
        <activity
            android:name=".MuscleActivity"
            android:parentActivityName=".MainMenuActivity"></activity>
        <activity
            android:name=".WorkoutActivity"
            android:parentActivityName=".MuscleActivity"></activity>
        <activity
            android:name=".ExerciseActivity"
            android:parentActivityName=".WorkoutActivity"></activity>
    </application>

</manifest>